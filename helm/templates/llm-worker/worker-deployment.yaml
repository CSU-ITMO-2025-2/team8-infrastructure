apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "team8-app.fullname" . }}-llm-worker
  labels:
    {{- include "demo-worker.labels" . | nindent 4 }}
    app.kubernetes.io/component: llm-worker
spec:
  replicas: {{ .Values.worker.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "demo-worker.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: llm-worker
  template:
    metadata:
      {{- with .Values.worker.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "demo-worker.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: llm-worker
    spec:
      terminationGracePeriodSeconds: {{ .Values.worker.terminationGracePeriodSeconds | default 60 }}
      securityContext:
        {{- toYaml (coalesce .Values.worker.podSecurityContext .Values.podSecurityContext) | nindent 8 }}
      containers:
        - name: llm-worker
          securityContext:
            {{- toYaml (coalesce .Values.worker.securityContext .Values.securityContext) | nindent 12 }}
          image: "{{ .Values.worker.image.repository }}:{{ .Values.worker.image.tag }}"
          imagePullPolicy: {{ .Values.worker.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.worker.httpPort | default 8080 }}
              protocol: TCP

          envFrom:
            - secretRef:
                name: hf-credentials

          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.worker.kafka.bootstrapServers | quote }}
            - name: KAFKA_TOPIC
              value: {{ .Values.worker.kafka.topic | quote }}
            - name: KAFKA_CONSUMER_GROUP
              value: {{ .Values.worker.kafka.consumerGroup | quote }}
            {{- if or (not (hasKey .Values.worker "hfCache")) (.Values.worker.hfCache.enabled | default true) }}
            - name: HF_HOME
              value: {{ .Values.worker.hfCache.mountPath | default "/var/cache/huggingface" | quote }}
            - name: TRANSFORMERS_CACHE
              value: {{ .Values.worker.hfCache.mountPath | default "/var/cache/huggingface" | quote }}
            {{- end }}
            {{- with .Values.worker.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}

          livenessProbe:
            httpGet:
              path: {{ .Values.worker.probes.liveness.path | default "/health/live" }}
              port: http
            initialDelaySeconds: {{ .Values.worker.probes.liveness.initialDelaySeconds | default 10 }}
            periodSeconds: {{ .Values.worker.probes.liveness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.worker.probes.liveness.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.worker.probes.liveness.failureThreshold | default 3 }}
          readinessProbe:
            httpGet:
              path: {{ .Values.worker.probes.readiness.path | default "/health/ready" }}
              port: http
            initialDelaySeconds: {{ .Values.worker.probes.readiness.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.worker.probes.readiness.periodSeconds | default 5 }}
            timeoutSeconds: {{ .Values.worker.probes.readiness.timeoutSeconds | default 3 }}
            failureThreshold: {{ .Values.worker.probes.readiness.failureThreshold | default 3 }}

          {{- with .Values.worker.probes.startup }}
          {{- if or (not (hasKey $.Values.worker.probes "startup")) (.enabled | default true) }}
          startupProbe:
            httpGet:
              path: {{ .path | default "/health/live" }}
              port: http
            periodSeconds: {{ .periodSeconds | default 10 }}
            failureThreshold: {{ .failureThreshold | default 18 }}
          {{- end }}
          {{- end }}

          lifecycle:
            {{- if or (not (hasKey .Values.worker "lifecycle")) (.Values.worker.lifecycle.preStop.enabled | default true) }}
            preStop:
              httpGet:
                path: {{ .Values.worker.lifecycle.preStop.path | default "/shutdown" }}
                port: http
            {{- end }}

          volumeMounts:
            {{- if or (not (hasKey .Values.worker "tmpDir")) (.Values.worker.tmpDir.enabled | default true) }}
            - name: tmp-dir
              mountPath: {{ .Values.worker.tmpDir.mountPath | default "/tmp" }}
            {{- end }}
            {{- if or (not (hasKey .Values.worker "hfCache")) (.Values.worker.hfCache.enabled | default true) }}
            - name: hf-cache
              mountPath: {{ .Values.worker.hfCache.mountPath | default "/var/cache/huggingface" }}
            {{- end }}

          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}

      {{- with .Values.worker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- $paa := .Values.worker.podAntiAffinity | default dict -}}
      {{- if or ($paa.enabled | default false) .Values.worker.affinity }}
      affinity:
        {{- if ($paa.enabled | default false) }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: {{ include "demo-worker.name" . }}
                  app.kubernetes.io/component: llm-worker
              topologyKey: {{ $paa.topologyKey | default "kubernetes.io/hostname" }}
        {{- end }}
        {{- with .Values.worker.affinity }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}

      {{- with .Values.worker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      volumes:
        {{- if or (not (hasKey .Values.worker "tmpDir")) (.Values.worker.tmpDir.enabled | default true) }}
        - name: tmp-dir
          emptyDir:
            {{- if .Values.worker.tmpDir.sizeLimit }}
            sizeLimit: {{ .Values.worker.tmpDir.sizeLimit }}
            {{- end }}
        {{- end }}
        {{- if or (not (hasKey .Values.worker "hfCache")) (.Values.worker.hfCache.enabled | default true) }}
        - name: hf-cache
          emptyDir:
            {{- if .Values.worker.hfCache.sizeLimit }}
            sizeLimit: {{ .Values.worker.hfCache.sizeLimit }}
            {{- end }}
        {{- end }}
